export interface linksType {
  id: number;
  path: string;
  text: string;
}

export interface DataType {
  id: number;
  numero: string;
  designation: string;
  // niveau: string;
  create_at: string;
  update_at: string;
  zip: null;
}

export interface IUser {
  first_name: string;
  last_name: string;
  email: string;
}
